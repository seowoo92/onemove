# 개발일지 2026-07-14 (화) — 2차 스프린트 2일차

> 알림 시스템 완성(오후 리마인더 + 스케줄), PWA 홈 화면 설치, Solar 키 은닉, 루틴 70개 확장 + 매일 루틴(★). 이날 마감 시점에 맥 데스크탑 → 리눅스 서버로 작업 환경 이관 준비.

## 오후 미완료 리마인더 완성 (#7)
- Edge Function `remind-incomplete` 배포 + Secrets(CRON_SECRET) + pg_cron 예약(`remind-incomplete-daily`, 매일 09:00 UTC = 18:00 KST)
- 트러블슈팅: 함수 Verify JWT 옵션이 켜져 있어 401 → OFF로 해결 (send-kakao와 동일 패턴)
- 수동 트리거 테스트: `{"ok":true,"date":"2026-07-14","sent":1}` — 실기기 카톡 수신 확인
- 부수 효과: 매일 크론 실행으로 Supabase 무료플랜 7일 미사용 자동 정지 원천 방지

## 쉬어가기 멘트 톤 교정
- 문제: "쉬어가기 결정을 멋지게 해냈네요! 오늘의 승리예요" — 쉬는 것을 성취로 축하 → 가식적
- 원칙 추가: 쉬는 건 잘한 일도 못한 일도 아닌 그냥 괜찮은 일. 축하 금지, 안심만
- 상황 라벨 명확화: "루틴 하나를 쉬는 것"(하루 전체 아님) — AI의 과잉 일반화 방지
- 3코치 예시·예비 메시지 교체, 실 API 검증 완료

## PWA 홈 화면 설치 (#12)
- manifest.webmanifest(standalone·크림 테마) + 앱 아이콘 192px 생성 + iOS 메타 태그
- 아이폰 사파리 실기기 확인: 주소창 없는 전체화면 앱으로 실행
- Web Push는 스코프대로 미포함 (알림은 카카오가 전담)

## Solar API 키 은닉 (#13)
- Edge Function `solar-chat` 프록시: 키는 Supabase Secrets(UPSTAGE_API_KEY)에만, 번들에서 완전 소멸
- 남용 제한: 모델 고정·max_tokens 상한·메시지 형식 검증. 실패 시 예비 메시지 폴백(게스트 포함 무중단)
- deploy.yml에서 VITE_SOLAR_API_KEY 제거. 라이브 검증: 게스트 흐름에서 출처·AI 메시지 수신
- 남은 것: 업스테이지 키 재발급·구키 폐기 (7/26 예정, 과거 번들 노출 이력 정리)

## 루틴 70개 확장 + 매일 루틴(★) (#15)
- 루틴 28 → 70개 (영역당 10개). 신규 42개 문구는 사용자 검수 3회 왕복으로 확정
  - 검수 기준으로 정립: 물건·재고 가정 금지 / 시간대 제약 금지 / 애매한 동작 반복 지시 지양 / 감성·오글 표현 배제 / "쉬기"가 아닌 행동
- routine_19: '창문 열고 3분 환기' → '창문 열고 10분 환기하기' 업그레이드
- 매일 루틴: 카드의 별(★) 토글로 최대 3개 고정 — 항상 추천 맨 앞에 포함, 어제 제외 룰 미적용, 남는 슬롯만 랜덤. profiles.pinned_ids로 기기 간 동기화
- 검증: 좋아요(4개) 추천에 신규 루틴 등장, 고정 후 날씨 재선택(힘들어요 2슬롯) 시 고정 루틴 유지 확인

## 발견·기록
- 카카오 '나와의 채팅'은 푸시 알림·안읽음 배지가 없음(카카오 정책) — 카드는 '조용한 보관함' 역할. 울리는 알림 필요 시 Web Push 재검토 (7/18쯤 판단)
- 피드 템플릿은 본문 2줄만 표시 → 텍스트 템플릿으로 전환(전문 표시)
- 메시지 버튼 동작에는 카카오 앱 Web 플랫폼 도메인 등록 필요 (seowoo92.github.io 등록 완료)
- 기기 간 프로필 동기화는 "최신 시간 비교" 방식 (7/13 닉네임 불일치 버그의 해결책)

## 대기 중 확인
- profiles.pinned_ids 컬럼 ALTER 실행 여부 확인 필요 (supabase/schema.sql 마지막 줄, Table Editor에서 확인)

## 환경 이관
- 이날 마감 시점부로 작업 환경을 맥 데스크탑 → 리눅스 서버로 이관. 셋업·계정·규칙은 docs/handoff-linux.md 참조
